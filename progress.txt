## Codebase Patterns
- pnpmワークスペース: `packages/core/`, `packages/react/`, `examples/`
- TypeScript: strict mode, ESM, `verbatimModuleSyntax`, project references
- テスト: vitest, coverage v8, テストファイルは `packages/*/tests/`
- lint: ESLint flat config + typescript-eslint strict + @luma-dev/eslint-plugin-luma-ts (全ルール有効)
- format: Prettier
- scripts: `lint`, `lint-fix`, `test`, `test:run`, `coverage`, `typecheck`
- tsconfigのプロジェクト参照: テスト用は `tsconfig.test.json`, ルート設定用は `tsconfig.node.json`
- EffectResult: `Data.struct` で構造的等値性、型ガードは TypeScript 推論 (`no-explicit-return-is`)
- テンプレートリテラル内の式: `satisfies string` が必要 (`luma-ts/require-satisfies-in-tls`)
- `as unknown as T` 禁止 (`luma-ts/no-as-unknown-as`): delegate パターンで型安全に変換
- EffectStore: `StoreRuntime` インターフェースで ManagedRuntime に依存しない抽象化
- ManagedRuntime テスト: `ManagedRuntime.make(Layer.empty)` で空のランタイム作成
- EffectObserver: subscribe→run の順序で通知を漏らさない（store.subscribeを先に呼ぶ）
- React テスト: `@vitest-environment jsdom` コメント + `@testing-library/react` の `renderHook`/`render`
- React Context 型消去: `createContext<any>` + フック呼び出し時の `<R, E>` 型指定
- StoreRuntime: Fiber エラー型を `unknown` にすることで ManagedRuntime<R, E> と互換
- EffectProvider: EffectStore も Context で提供。runtime と同じライフサイクルで作成・dispose
- useEffectQuery: `useSyncExternalStore` + `createEffectObserver` の組み合わせ。key でキャッシュ共有
- Suspense フック: Promise を observer に自己購読させてサスペンド中でも resolve する。`EffectError<E>` で ErrorBoundary にキャッチ
- `Error` サブクラスの `cause` プロパティは ES2022 `Error.cause` と衝突するため `override` 必須
- React `Component` サブクラスの `render()` も `override` 必須（TypeScript strict + noImplicitOverride）
- useEffectMutation: runtime 直接利用 + `createSubscribable` でコンポーネントローカル管理。EffectStore 不使用
- mutation の observer fiber は直接キャンセル不要。mutation fiber interrupt → Fiber.await が Interrupt exit → ガードで無視
- useEffectStream: `Stream.runForEach` + `Effect.catchAll` でストリーム消費。interrupt は自然伝搬、typed errors のみ catch
- Stream テスト: `createControllableStream` ヘルパーで `emit/end/fail` を外部制御（`Stream.async` の Emit API を型安全にラップ）
- useEffectScope: `Effect.runSync` で Scope 操作（ManagedRuntime 不要）。`useEffect` 内でScope作成・close
- EffectBoundary: Suspense + ErrorBoundary 統合。`renderError` 未指定時はエラー伝搬。非 EffectError は常に親に伝搬

## 2026-03-03 - 初期セットアップ
- 実装した内容:
  - pnpmワークスペース + モノレポ構成
  - TypeScript strict mode, ESM, project references
  - vitest設定 + カバレッジ (v8, 100%閾値)
  - ESLint flat config + typescript-eslint strict + @luma-dev/eslint-plugin-luma-ts 全ルール有効
  - Prettier設定
  - packages/core/ と packages/react/ のスケルトン (空のindex.ts + 基本テスト)
  - Effect.ts ^3.19.19 依存追加 (packages/core, packages/react)
  - CIセットアップタスクファイル (tasks/06-ci-setup.md) 作成
  - ドキュメントタスクファイル (tasks/07-documentation.md) 作成
- 変更したファイル:
  - package.json, pnpm-workspace.yaml, tsconfig.json, tsconfig.node.json
  - vitest.config.ts, eslint.config.js, .prettierrc, .prettierignore, .gitignore
  - packages/core/ (package.json, tsconfig.json, tsconfig.test.json, src/index.ts, tests/index.test.ts)
  - packages/react/ (package.json, tsconfig.json, tsconfig.test.json, src/index.ts, tests/index.test.ts)
  - tasks/06-ci-setup.md, tasks/07-documentation.md
- カバレッジ: 0% (実装コードなし、index.tsはexclude対象)
- **今後のイテレーションへの学び:**
  - テストファイルはtsconfigに含める必要がある → 各パッケージに `tsconfig.test.json` を用意
  - ESLintのprojectServiceはtsconfigに含まれないファイルをパースできない
  - vitest.config.tsなどのルートファイル用に `tsconfig.node.json` が必要
  - `pnpm approve-builds` は対話的UIのため、`package.json` の `pnpm.onlyBuiltDependencies` で設定する
---

## 2026-03-03 - EffectResult 型定義 (タスク 1.2)
- 実装した内容:
  - `EffectResult<A, E>` discriminated union 型定義 (Initial, Pending, Success, Failure, Refreshing)
  - コンストラクタ関数 (`initial`, `pending`, `success`, `failure`, `refreshing`) - `Data.struct` で構造的等値性
  - 型ガード関数 (`isInitial`, `isPending`, `isSuccess`, `isFailure`, `isRefreshing`)
  - exhaustive match ヘルパー (`matchEffectResult`)
  - ユーティリティ関数 (`hasValue`, `getValue`)
  - 31テスト (構造的等値性、型ガード、exhaustive match、hasValue、getValue)
- 変更したファイル:
  - packages/core/src/EffectResult.ts (新規)
  - packages/core/src/index.ts (re-export追加)
  - packages/core/tests/EffectResult.test.ts (新規)
  - packages/core/tests/index.test.ts (修正: dynamic import → static import)
- カバレッジ: 0% → 100% (EffectResult.ts)
- **今後のイテレーションへの学び:**
  - `luma-ts/no-explicit-return-is`: 型ガードの `result is T` を明示しない。TypeScript 5.5+ の推論に任せる
  - `luma-ts/require-satisfies-in-tls`: テンプレートリテラル内の式に `satisfies string` 等が必要
  - `Data.struct` はシンプルなオブジェクトに構造的等値性を付与。`Data.TaggedEnum` はジェネリック版では `$is`/`$match` が使えないため、手動定義の方が柔軟
  - Effect.ts の動的 import はvitest のタイムアウト (5s) を超えうる。static import を使用すべき
---

## 2026-03-03 - Subscribable インターフェース (タスク 1.3)
- 実装した内容:
  - `Subscribable<A>` インターフェース (`useSyncExternalStore` 互換: `subscribe` + `getSnapshot`)
  - `MutableSubscribable<A>` インターフェース (`Subscribable<A>` + `set`)
  - `createSubscribable<A>` ファクトリ関数 (参照等価性チェック付き)
  - 14テスト (subscribe/unsubscribe、参照等価性 Object.is、NaN/±0 エッジケース、複数subscriber、ダブルunsubscribe)
- 変更したファイル:
  - packages/core/src/Subscribable.ts (新規)
  - packages/core/src/index.ts (re-export追加)
  - packages/core/tests/Subscribable.test.ts (新規)
- カバレッジ: 100% → 100% (Subscribable.ts 100%)
- **今後のイテレーションへの学び:**
  - `Subscribable` は React 非依存で、`useSyncExternalStore` の `subscribe` + `getSnapshot` と同じシグネチャ
  - `Object.is` は `===` と異なり NaN === NaN (true)、+0 === -0 (false) を区別する
  - `Set<() => void>` で listener 管理すると、同じ関数の二重登録を防ぎつつ O(1) の追加・削除が可能
---

## 2026-03-03 - EffectStore コア実装 (タスク 1.4)
- 実装した内容:
  - `EffectStore` インターフェース: `getSubscribable`, `run`, `invalidate`, `subscribe`, `getSnapshot`, `dispose`
  - `StoreRuntime` インターフェース: ManagedRuntime互換の最小ランタイムインターフェース
  - `createEffectStore` ファクトリ関数: ランタイム + 設定を受け取り EffectStore を生成
  - キーベースのキャッシュ管理: 各キーごとに `MutableSubscribable<EffectResult<A, E>>` で状態管理
  - 状態遷移: Initial → Pending → Success/Failure、Success → Refreshing → Success
  - Fiber管理: 同一キー再実行時に前のFiberをinterrupt、dispose時に全Fiberをinterrupt
  - GCタイマー: 設定可能な猶予期間（デフォルト30秒）、unsubscribe後にエントリを削除
  - invalidate: 最後に実行したEffectを再実行
  - 23テスト (状態遷移、Fiber管理、invalidate、subscribe、GCタイマー、複数キー、dispose)
- 変更したファイル:
  - packages/core/src/EffectStore.ts (新規)
  - packages/core/src/index.ts (re-export追加)
  - packages/core/tests/EffectStore.test.ts (新規)
  - tasks/01-foundation.md (タスク1.4を完了マーク)
- カバレッジ: 100% → 100% (EffectStore.ts 100%)
- **今後のイテレーションへの学び:**
  - `luma-ts/no-as-unknown-as`: `as unknown as T` は禁止。delegate パターン（新しいオブジェクトでラップ）で型安全に変換
  - `Fiber.interruptFork` は fire-and-forget の interrupt に適する。`Fiber.interrupt` は await する場合に使用
  - `Exit.match` で Success/Failure を分岐するのが最もクリーン
  - ManagedRuntime の `runFork` は Effect を fork して Fiber を返す。テストでは `ManagedRuntime.make(Layer.empty)` で空のランタイムを作成
  - GCタイマーのテストでは `vi.useFakeTimers()` / `vi.advanceTimersByTime()` が効果的
  - 非同期Effect完了の待機には `vi.waitFor()` が便利
---

## 2026-03-03 - EffectObserver 実装 (タスク 1.5)
- 実装した内容:
  - `createEffectObserver` ファクトリ関数: EffectStore + キー + Effect → `Subscribable<EffectResult<A, E>>`
  - Lazy acquisition: 最初の subscribe で `store.run(key, effect)` を実行
  - 2回目以降の subscribe ではデータがキャッシュ済みなら再実行しない
  - 最後の unsubscribe で EffectStore 側の GC に委譲
  - subscriberCount による参照カウント管理
  - double unsubscribe の安全性（冪等）
  - 11テスト（lazy acquisition、遷移シーケンス、エラー、GC、re-subscribe、複数subscriber）
- 変更したファイル:
  - packages/core/src/EffectObserver.ts (新規)
  - packages/core/src/index.ts (re-export追加)
  - packages/core/tests/EffectObserver.test.ts (新規)
  - tasks/01-foundation.md (タスク1.5を完了マーク)
- カバレッジ: 100% → 100% (EffectObserver.ts 100%)
- **今後のイテレーションへの学び:**
  - Observer の subscribe 内で store.subscribe を store.run **より前に**呼ぶことで、Pending 通知を漏らさない
  - EffectObserver は EffectStore の薄いラッパーであり、GC・Fiber管理はすべて EffectStore に委譲できる
  - `isInitial` を使ってキャッシュ有無を判定し、re-subscribe 時の不要な再実行を防止
---

## 2026-03-03 - EffectProvider 実装 (タスク 2.1)
- 実装した内容:
  - `EffectProvider` コンポーネント: React Context で ManagedRuntime をツリーに提供
  - `useEffectRuntime()` フック: Provider 外で使用時に明確なエラーメッセージ
  - `EffectManagedRuntime<R, E>` インターフェース: ManagedRuntime の最小公開インターフェース
  - Layer prop → useEffect で ManagedRuntime 生成、cleanup で dispose
  - Layer 変更時にランタイムを再生成（旧ランタイムは dispose）
  - 5テスト (Provider外エラー、ランタイム取得、unmount時dispose、Layer変更時再生成、サービスアクセス)
- 変更したファイル:
  - packages/react/src/EffectProvider.tsx (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/EffectProvider.test.tsx (新規)
  - packages/react/package.json (devDependencies: react, @types/react, react-dom, @types/react-dom, @testing-library/react)
  - package.json (devDependencies: jsdom)
  - vitest.config.ts (.tsx テストパターン追加)
  - eslint.config.js (.trash/ を ignore に追加)
- カバレッジ: 100% → 100% (EffectProvider.tsx 100%)
- **今後のイテレーションへの学び:**
  - React Context で型消去が必要: `createContext<any>` + `useEffectRuntime<R, E>()` で呼び出し時に型指定。`@typescript-eslint/no-unsafe-return` の eslint-disable が必要
  - `@testing-library/react` の `renderHook` で wrapper の props を動的に変えるには `initialProps` + wrapper の追加 props パターンを使う。ただし `render` + カスタムコンポーネントの方が制御しやすいケースもある
  - `ManagedRuntime.make(Layer.empty)` は vitest + jsdom 環境で問題なく動作する。ただし `Layer.mergeAll(Layer.empty)` は `_op_layer` エラーを起こすことがある
  - vitest で jsdom 環境を使うには `@vitest-environment jsdom` コメントをテストファイル先頭に追加する
  - React テスト用に `.tsx` ファイルのテストパターンを `vitest.config.ts` の include に追加する必要がある
---

## 2026-03-03 - useEffectQuery フック実装 (タスク 2.2)
- 実装した内容:
  - `useEffectQuery` フック: `useSyncExternalStore` + `createEffectObserver` で Effect 実行結果を購読
  - EffectProvider 拡張: EffectStore を Context 経由で子コンポーネントに提供
  - `useEffectStore` フック: Provider 内部の EffectStore を取得
  - `StoreRuntime` インターフェース修正: Fiber エラー型を `unknown` に変更（ManagedRuntime<R, E> 互換）
  - 8テスト追加（状態遷移 sync/async、Failure、キー変更、unmount、StrictMode、キャッシュ共有、useEffectStore エラー）
- 変更したファイル:
  - packages/react/src/useEffectQuery.ts (新規)
  - packages/react/src/EffectProvider.tsx (EffectStore Context追加、useEffectStore追加)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/useEffectQuery.test.tsx (新規)
  - packages/react/tests/EffectProvider.test.tsx (useEffectStore エラーテスト追加)
  - packages/core/src/EffectStore.ts (StoreRuntime.runFork の戻り値型修正)
- カバレッジ: 100% → 100%
- **今後のイテレーションへの学び:**
  - `ManagedRuntime<R, E>.runFork<A, EffE>` は `RuntimeFiber<A, EffE | LayerE>` を返す。StoreRuntime で `unknown` にすることで型互換性が解決
  - `Effect.async<T>((cb) => { ... })` でコールバック駆動の非同期テストが簡潔に書ける（Deferred.unsafeMake は API が変わりやすい）
  - ESLint `@typescript-eslint/await-thenable`: `act(() => { ... })` が void を返す場合は `await` 不要
  - ESLint `@typescript-eslint/no-unnecessary-type-parameters`: ヘルパー関数で型パラメータが1箇所しか使われない場合はエラー
  - `exactOptionalPropertyTypes` が有効なので、関数に余分なプロパティを持つオブジェクトを渡せない
---

## 2026-03-03 - useEffectSuspense フック実装 (タスク 2.3)
- 実装した内容:
  - `useEffectSuspense<A, E>` フック: Suspense/ErrorBoundary 統合版の Effect 実行フック
  - `EffectError<E>` クラス: Failure 時に throw → ErrorBoundary でキャッチ可能。`Cause<E>` を保持
  - Pending 時に Promise を throw → Suspense fallback 表示
  - Success/Refreshing 時は値 `A` を直接返す（Refreshing 中は再サスペンドしない）
  - Promise は observer に自己購読する仕組みで、サスペンド中（コンポーネントがアンマウント状態）でも解決可能
  - key 変更時に Promise キャッシュを無効化
  - 8テスト（Suspense async/sync、ErrorBoundary catch/cause、Promise安定性、StrictMode、Refreshing維持、EffectError properties）
- 変更したファイル:
  - packages/react/src/useEffectSuspense.ts (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/useEffectSuspense.test.tsx (新規)
  - tasks/02-react-integration.md (タスク2.3を完了マーク)
- カバレッジ: 100% → 100% (useEffectSuspense.ts 100%)
- **今後のイテレーションへの学び:**
  - Suspense で throw した Promise はコンポーネントがサスペンドしている間に resolve される必要がある。`useSyncExternalStore` のコールバックはサスペンド中に呼ばれないため、Promise 自体に observer の subscribe を組み込んで自己解決させる
  - `Error` サブクラスで `cause` プロパティを定義すると ES2022 `Error.cause` と衝突する → `override` 修飾子が必要
  - React `Component` サブクラスの `render()` も `override` が必要（TypeScript `noImplicitOverride`）
  - `act(() => { ... })` は同期関数を渡す場合 `await` 不要。`async` ラッパーは ESLint `@typescript-eslint/require-await` エラーになる
  - テストで配列の要素にアクセスする場合 `arr[0]` は `T | undefined` になる（`noUncheckedIndexedAccess`）→ ガード文が必要
  - カバレッジ100%を達成するために、防御的なコードパス（到達不可能な分岐）は削除するか、到達可能にする設計に変更する
---

## 2026-03-03 - useEffectMutation フック実装 (タスク 2.4)
- 実装した内容:
  - `useEffectMutation<I, A, E>` フック: 手動トリガー型の Effect 実行
  - `MutationState<I, A, E>` インターフェース: `result`, `mutate(input)`, `reset()` を提供
  - EffectStore を使わず、`useEffectRuntime` + `createSubscribable` でコンポーネントローカルな状態管理
  - EffectResult を再利用: Initial = idle, Pending = pending, Success = success, Failure = failure
  - `mutate(input)` で Effect を生成・実行、前の mutation fiber を自動キャンセル
  - `reset()` で状態を Initial に戻し、実行中 fiber をキャンセル
  - unmount 時の cleanup（実行中 fiber の interrupt）
  - 11テスト（idle状態、sync/async遷移、Failure、引数付きmutate、reset、reset中キャンセル、連続mutateキャンセル、unmount cleanup、StrictMode互換）
- 変更したファイル:
  - packages/react/src/useEffectMutation.ts (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/useEffectMutation.test.tsx (新規)
  - tasks/02-react-integration.md (タスク2.4を完了マーク)
- カバレッジ: 100% → 100% (useEffectMutation.ts 100%)
- **今後のイテレーションへの学び:**
  - mutation フックは EffectStore を経由せず runtime 直接利用が適切。EffectStore はクエリのキャッシュ共有用で、mutation はコンポーネントローカル
  - `MutationInternals` の `subscribable.set` が contravariant なため、ジェネリック関数に `MutationInternals<unknown, unknown>` を渡すと型エラー。`Pick<..., "fiber">` で必要なフィールドのみを受け取るか、別インターフェースに分離する
  - observer fiber は mutation fiber の `Fiber.await` 結果を待つだけなので、直接キャンセルする必要はない。mutation fiber をinterruptすれば observer は Interrupt exit を受け取り、`internals.fiber !== fiber` ガードで安全に無視される（EffectStore と同じパターン）
  - `_input: void` は ESLint `@typescript-eslint/no-invalid-void-type` エラー → `_input: undefined` を使用
---

## 2026-03-03 - useEffectStream フック実装 (タスク 2.5)
- 実装した内容:
  - `useEffectStream<A, E>` フック: Effect.ts Stream を購読し最新の値を返す
  - `StreamResult<A, E>` インターフェース: `result: EffectResult<A, E>` を提供
  - `Stream.runForEach` で値を消費、各値で `subscribable.set(success(value))` で状態更新
  - `Effect.catchAll` で typed errors のみを catch → `failure(Cause.fail(error))` に遷移
  - Interrupt（unmount/stream変更）は自然に伝搬し、状態を更新しない
  - EffectStore を使わず runtime + createSubscribable でコンポーネントローカル管理（mutation と同じパターン）
  - Pull-based backpressure: `runForEach` の特性で自然にバックプレッシャーが効く
  - 10テスト（有限Stream完了、async Pending→Success、Failure、sequential async、unmount cleanup、post-unmount安全性、stream completion保持、empty stream、error after values、StrictMode）
- 変更したファイル:
  - packages/react/src/useEffectStream.ts (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/useEffectStream.test.tsx (新規)
  - tasks/02-react-integration.md (タスク2.5を完了マーク)
- カバレッジ: 100% → 100% (useEffectStream.ts 100%)
- **今後のイテレーションへの学び:**
  - Stream フックは mutation と同じくコンポーネントローカル管理が適切。EffectStore はクエリキャッシュ用
  - `Stream.async` の `Emit` API: `emitter.single(value)`, `emitter.end()`, `emitter.fail(error)` が型安全
  - `Effect.catchAll` は typed errors (E) のみを catch し、interrupt は自然に伝搬。`Effect.catchAllCause` は interrupt も catch してしまうため不適切
  - `Effect.exit` + `Cause.isInterruptedOnly` パターンは interrupt が fire-and-forget の場合カバレッジが取りにくい。`Effect.catchAll` の方がシンプルで100%カバレッジ達成しやすい
  - テストで `Stream.async` の emitter をテスト側で制御する場合、`createControllableStream` ヘルパーパターンが有効（型安全にemit/end/failを外部制御）
---

## 2026-03-03 - useEffectScope フック実装 (タスク 2.6)
- 実装した内容:
  - `useEffectScope` フック: コンポーネントライフサイクルに紐づく Effect.ts Scope
  - `ScopeHandle` インターフェース: `addFinalizer(finalizer: Effect.Effect<void>)` を提供
  - mount時（useEffect内）に `Scope.make()` でスコープ作成
  - unmount時に `Scope.close(scope, Exit.void)` で全ファイナライザ実行
  - ファイナライザはLIFO順序（最後に登録されたものが最初に実行）
  - unmount後の `addFinalizer` は安全にno-op
  - Scope操作は `Effect.runSync` を直接使用（ManagedRuntimeのサービスコンテキスト不要）
  - 7テスト（ScopeHandle取得、unmount時実行、複数ファイナライザ、LIFO順序、空scope unmount、unmount後no-op、StrictMode）
- 変更したファイル:
  - packages/react/src/useEffectScope.ts (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/useEffectScope.test.tsx (新規)
  - tasks/02-react-integration.md (タスク2.6を完了マーク)
- カバレッジ: 100% → 100% (useEffectScope.ts 100%)
- **今後のイテレーションへの学び:**
  - Scope操作（`Scope.make`, `Scope.addFinalizer`, `Scope.close`）は純粋な操作であり、`Effect.runSync` で直接実行可能。ManagedRuntimeの `runSync`/`runFork` を経由する必要がない
  - `useMemo` でScope作成すると、React render中の副作用になりうる。`useEffect` 内で作成する方が安全
  - `vi.fn()` のモックは `any` を返すため、`Effect.sync(() => fn())` はESLintの `no-unsafe-return` エラーになる。ブロック構文 `Effect.sync(() => { fn(); })` で回避
  - `useEffectRuntime()` を呼ぶだけでProvider外使用のガードになる（戻り値を使わなくても検証は走る）
---

## 2026-03-03 - EffectBoundary コンポーネント実装 (タスク 2.7)
- 実装した内容:
  - `EffectBoundary<E>` コンポーネント: Suspense + ErrorBoundary の統合コンポーネント
  - `fallback` prop: Suspense の fallback（loading中の表示）
  - `renderError` prop (optional): EffectError をキャッチして `Cause<E>` を受け取るレンダー関数
  - `renderError` 未指定時は EffectError が親の ErrorBoundary に伝搬
  - 非 EffectError は常に親の ErrorBoundary に伝搬（EffectBoundary では捕捉しない）
  - ネストされた EffectBoundary で内側が独自のエラーハンドリングを持つケース対応
  - 7テスト（Suspense統合、renderErrorでCause取得、Causeパターンマッチ、エラー伝搬、ネスト、非EffectError伝搬、StrictMode）
- 変更したファイル:
  - packages/react/src/EffectBoundary.tsx (新規)
  - packages/react/src/index.ts (re-export追加)
  - packages/react/tests/EffectBoundary.test.tsx (新規)
  - tasks/02-react-integration.md (タスク2.7を完了マーク)
- カバレッジ: 100% → 100% (EffectBoundary.tsx 100%)
- **今後のイテレーションへの学び:**
  - React ErrorBoundary は class component でしか実装できない（getDerivedStateFromError が関数コンポーネントに未対応）
  - ErrorBoundary の re-throw: `getDerivedStateFromError` でキャッチしたエラーを render で re-throw すると親の ErrorBoundary に伝搬する。`@typescript-eslint/only-throw-error` の eslint-disable が必要（型が unknown のため）
  - EffectBoundary は Suspense と ErrorBoundary を組み合わせるため、ErrorBoundary が Suspense の外側にある必要がある（ErrorBoundary > Suspense > children の順序）
  - ネストされた Suspense テストでは、外側の Suspense が全子要素をブロックするため、外側のコンポーネントを同期 Effect にする必要がある
---
